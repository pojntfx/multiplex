using Gtk 4.0;
using Adw 1;

template $MultiplexErrorDialog: Adw.AlertDialog {
  heading: _("A Fatal Error Occurred");
  default-response: "close";
  close-response: "close";

  responses [
    report: _("Report Error"),
    close: _("Close Multiplex") destructive
  ]
}
